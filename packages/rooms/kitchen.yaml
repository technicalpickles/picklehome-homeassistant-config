script:
  kitchen_on:
    sequence:
      - service: hue.hue_activate_scene
        data:
          group_name: Kitchen
          scene_name: Read
  kitchen_off:
    sequence:
      - service: light.turn_off
        entity_id: light.kitchen
